// template é interlopação (interpretação)

const nome = "rebeca";
const concatenacao = "olá" + nome + "!";
const template = `Olá
                  ${nome}`;



// expressões...
console.log(`1 + 1 = ${1 + 1}`)

const up = text => text.toUpperCase()
console.log(`Ei... ${up("cuidado")}!`)